const baseUrl = '/images'

function logoSMP() {
  return `${baseUrl}/logo-smp.png`
}

function posterMuridBaru() {
  return `${baseUrl}/POSTER-MURID-BARU.png`
}

function logoFooter() {
  return `${baseUrl}/logo-3-unit-e1642467574309.png`
}

const images = {
  logoSMP,
  posterMuridBaru,
  logoFooter,
}

export default images
